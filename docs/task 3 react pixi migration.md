pixi react binding and pixi itself has updated, make the migration to latest version.

latest version of these packages are already installed.

create a markdown file in the docs folder to document the implementation details and reasoning and always keep it updated with latest change you made.
