with new fs api created in task-1, we need to replace existing usage involving fs in renderer, so that renderer could continue to access files on disks

create a markdown file in the docs folder to document the implementation details and reasoning.
