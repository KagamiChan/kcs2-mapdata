import React, { Component } from 'react'
import styled from 'styled-components'

const Wrapper = styled.div`
  grid-area: editor;
`

const Editor = () => <Wrapper>Editor</Wrapper>

export default Editor
